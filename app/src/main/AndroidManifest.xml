<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid_19tracker2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid19Tracker2021">
        <activity android:name=".covidcasesindia"
            android:theme="@style/Theme.AppCompat" ></activity>
        <activity
            android:name=".lastdataentry"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".districtActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".GUJARATCASES"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>